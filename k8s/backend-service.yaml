apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ead-ca201
spec:
  selector:
    app: backend
    version: blue
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP